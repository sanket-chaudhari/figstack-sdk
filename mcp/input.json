{
  "files": [
    {
      "path": "scripts/sandbox/test-fetch-frame.ts",
      "content": "// Test script to fetch all pages and frames from a Figma file\nimport { FigmaAPI } from '@core/api/figma-api.js';\nimport * as dotenv from 'dotenv';\n\ndotenv.config();\n\nconst run = async () => {\n  const fileKey = process.env.TEST_FILE_KEY!;\n  const token = process.env.FIGMA_PERSONAL_ACCESS_TOKEN;\n\n  if (!fileKey || !token) {\n    console.error('❌ Missing required env variables: TEST_FILE_KEY or FIGMA_PERSONAL_ACCESS_TOKEN.');\n    process.exit(1);\n  }\n\n  const figma = new FigmaAPI({ personalAccessToken: token });\n\n  try {\n    const file = await figma.getFile(fileKey);\n    console.log(`✅ Successfully fetched file: ${file.name}`);\n    const pages = file.document.children;\n\n    for (const page of pages) {\n      console.log(`📄 Page: ${page.name}`);\n      const frames = page.children?.filter((node: any) => node.type === 'FRAME');\n\n      if (frames?.length) {\n        for (const frame of frames) {\n          console.log(`  🖼️ Frame: ${frame.name} (ID: ${frame.id})`);\n        }\n      }\n    }\n  } catch (err) {\n    console.error('❌ Error fetching file from Figma:', err);\n  }\n};\n\nrun();"
    }
  ],
  "commitMessage": "fix(sandbox): update import in test-fetch-frame.ts to include .js extension for ESM compatibility"
}
